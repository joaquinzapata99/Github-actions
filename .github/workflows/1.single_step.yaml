name: First Action - Single

on:
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo "Hello world!"
  new-job:
    runs-on: ubuntu-latest
    needs: hello-world
    steps:
      - name: first-step
        run: echo "This is the new job!"
      - name: second-step
        run: echo "bye world!"